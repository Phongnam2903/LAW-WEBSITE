/**
 * Case management, backing the {@code cases}, {@code case_lawyers}, and {@code case_activities}
 * tables. Named {@code cases} rather than the master prompt's suggested {@code case} because
 * {@code case} is a reserved Java keyword and cannot be used as a package name.
 * Reserved for a later implementation phase — not implemented in Phase 3.
 */
package com.lawfirm.backend.cases;
